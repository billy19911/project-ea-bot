'use client';
import AppShell from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell activeKey="models" eyebrow="Xynn / Models" title="Models" actions={null}>
      <div className="p-4">Page under construction.</div>
    </AppShell>
  );
}
