'use client';
import AppShell from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell activeKey="reconciliation" eyebrow="Xynn / Reconciliation" title="Reconciliation" actions={null}>
      <div className="p-4">Page under construction.</div>
    </AppShell>
  );
}
