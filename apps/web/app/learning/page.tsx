'use client';
import AppShell from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell activeKey="learning" eyebrow="Xynn / Learning" title="Learning" actions={null}>
      <div className="p-4">Page under construction.</div>
    </AppShell>
  );
}
