'use client';
import AppShell from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell activeKey="decision-replay" eyebrow="Xynn / Decision Replay" title="Decision Replay" actions={null}>
      <div className="p-4">Page under construction.</div>
    </AppShell>
  );
}
