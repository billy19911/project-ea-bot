'use client';
import AppShell from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell activeKey="decisions" eyebrow="Xynn / Decisions" title="Decisions" actions={null}>
      <div className="p-4">Page under construction.</div>
    </AppShell>
  );
}
