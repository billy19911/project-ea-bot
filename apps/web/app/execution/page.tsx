'use client';
import AppShell from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell activeKey="execution" eyebrow="Xynn / Execution" title="Execution" actions={null}>
      <div className="p-4">Page under construction.</div>
    </AppShell>
  );
}
