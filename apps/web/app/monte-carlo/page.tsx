'use client';
import AppShell from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell activeKey="monte-carlo" eyebrow="Xynn / Monte Carlo" title="Monte Carlo" actions={null}>
      <div className="p-4">Page under construction.</div>
    </AppShell>
  );
}
