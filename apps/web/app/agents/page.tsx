'use client';
import AppShell from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell activeKey="agents" eyebrow="Xynn / Agents" title="Agents" actions={null}>
      <div className="p-4">Page under construction.</div>
    </AppShell>
  );
}
