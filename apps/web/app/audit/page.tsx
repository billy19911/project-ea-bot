'use client';
import AppShell from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell activeKey="audit" eyebrow="Xynn / Audit" title="Audit" actions={null}>
      <div className="p-4">Page under construction.</div>
    </AppShell>
  );
}
