'use client';
import AppShell from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell activeKey="system-health" eyebrow="Xynn / System Health" title="System Health" actions={null}>
      <div className="p-4">Page under construction.</div>
    </AppShell>
  );
}
