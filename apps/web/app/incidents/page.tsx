'use client';
import AppShell from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell activeKey="incidents" eyebrow="Xynn / Incidents" title="Incidents" actions={null}>
      <div className="p-4">Page under construction.</div>
    </AppShell>
  );
}
