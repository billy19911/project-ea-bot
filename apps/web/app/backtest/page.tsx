'use client';
import AppShell from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell activeKey="backtest" eyebrow="Xynn / Backtest" title="Backtest" actions={null}>
      <div className="p-4">Page under construction.</div>
    </AppShell>
  );
}
